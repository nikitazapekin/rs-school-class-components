/*
import { RootState } from './store';

export const getTest = (state: RootState) => state.app.test;
*/
import { RootState } from './store';

export const getTest = (state: RootState) => state.app.test;
