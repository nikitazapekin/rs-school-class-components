
import "./index.scss"
const StoredUsersButton = () => {
    return (
        <button className="stored__users" >
            Click to see your stored users
        </button>

    );
}

export default StoredUsersButton;